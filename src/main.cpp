#include <iostream>
#include "scene_graph/node.h"
#include "visualization/canvas.h"
#include "visualization/tree_view.h"

//using namespace scene_graph;

int main() {
    // std::cout << "Scene Graphs Application" << std::endl;
    
    // // Create a root node
    // auto root = std::make_shared<Node>("Root");
    
    // // Create canvas and tree view
    // Canvas canvas;
    // TreeView treeView;
    
    // // Set up the scene
    // canvas.addNode(root);
    // treeView.setRoot(root);
    
    // std::cout << "Scene graph created successfully!" << std::endl;
    return 0;
} 