#include "visualization/render_types.h"

namespace visualization {

// render_types.h is header-only and contains only data structures and enums.
// No implementation needed at this time.

// Add a dummy function to avoid "no symbols" error
bool initializeRenderTypes() {
    // Nothing to initialize, this function exists only to create a symbol
    return true;
}

}  // namespace visualization